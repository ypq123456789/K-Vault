Deployment rule:
- Do not leave Cloudflare Pages production-only changes as ad hoc deployments. Commit and push all code/configuration changes to GitHub after deployment so future Git-based builds match production.
